1. Примитивы i32 i64 u8 f64 bool — без разных размеров не сделать FFI к POSIX
2. FFI extern "C" + сырые указатели — открывает доступ ко всей libc
3. [u8] буферы фиксированного размера — read/write
4. Строки (ptr+len) с литералами и конкатенацией
5. Структуры